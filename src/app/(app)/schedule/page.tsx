import { ComingSoon } from "../_components/ui";

export default function Placeholder() {
  return (
    <ComingSoon
      icon="🗓️"
      title="Scheduling"
      description="Build and manage shifts across locations with a faster, friendlier workflow."
      badge="Phase 5"
    />
  );
}
